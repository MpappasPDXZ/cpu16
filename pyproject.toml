[project]
name = "kiewit-latex-docs"
version = "0.1.0"
description = "Kiewit LaTeX document compilation"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
build-pdf = "scripts.build:main"

[tool.uv]
dev-dependencies = []
